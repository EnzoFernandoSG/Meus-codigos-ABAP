REPORT yeg_yuyu.

DATA: bapi_fornecedor TYPE ekko-lifnr,
      bapi_compra     TYPE ekpo-ebeln, "ekpo-ebelp.
      bapi_item       TYPE ekpo-ebelp, "ekpo-ebeln,
      command(1)      TYPE c,
      count_doc_item  TYPE i.

DATA: vg_belnr LIKE mseg-belnr.
DATA: vg_gjahr LIKE mseg-gjahr.

INCLUDE: yeg_in_mvc_scr_yuyu,
         yeg_in_mvc_model_yuyu,
         yeg_in_mvc_view_yuyu,
         yeg_in_mvc_controller_yuyu.

DATA: eg_gc_controller TYPE REF TO report_controller.

INITIALIZATION.
  CREATE OBJECT eg_gc_controller.
  eg_gc_controller->initialization( ).

START-OF-SELECTION.
  eg_gc_controller->start_of_selection( ).

END-OF-SELECTION.
  eg_gc_controller->end_of_selection( ).
